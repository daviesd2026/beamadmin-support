# BeamAdmin Work Log - 2026-06-04

This note records the BeamAdmin and BeamMP automation work completed today.

## Automation Context

- Work was automated from the local PC and VPS using Codex.
- The session used `--dangerously-bypass-approvals-and-sandbox` to automate file edits, SSH/SCP deploys, GitHub releases, process cleanup, and server administration without approval prompts.
- VPS access used SSH/SCP with the local SSH key supplied by the owner.

## Admin Panel Deployment

- Deployed the BeamNG/BeamMP admin panel under `/opt/beamadmin/`.
- Kept BeamMP server files separate unless explicitly moving mod zip files.
- Confirmed Nginx serves the panel on port `8080` and proxies `/api/` to the Python API on `127.0.0.1:8091`.
- Kept static UI files in `/opt/beamadmin/`: `index.html`, `style.css`, `app.js`.
- Kept backend files in `/opt/beamadmin/`: `server.py`, `beamadmin_bridge.lua`.

## API And Bridge

- Built and deployed `server.py` for server status, auth, kick/ban actions, settings, maps, logs, console, mods, and restart controls.
- Installed/checked the BeamMP Lua bridge plugin path:
  `/opt/beammp-<id>/Resources/Server/beamadmin_bridge/beamadmin_bridge.lua`
- The bridge writes `status.json` and reads `queue.json` for player state and queued admin actions.
- Added normalization so live player data works even if bridge players are object-shaped instead of array-shaped.

## Dashboard And Player Admin

- Added live server cards and live player table.
- Added kick/ban controls with reason templates.
- Added BeamMP ID / server player ID visibility so admins can see who they are acting on.
- Added Discord link in admin reason/template flow:
  `https://discord.gg/2WEntWFeQs`

## Management Pages

Built out the six previously placeholder pages:

- Vehicles page
  - Lists spawned vehicles across servers.
  - Adds delete vehicle action.
- Maps page
  - Shows server map cards.
  - Map changes update config and restart the target server.
- Ban List page
  - Lists banned players and supports unban.
- Settings page
  - Edits server name, max players, password, private toggle.
  - Supports admin password change.
- Console page
  - Sends commands and shows recent output.
- Logs page
  - Shows per-server and all-server logs with colour coding and download.

## Server Restart Button

- Added a per-server `Restart` button to each dashboard server card.
- Added backend endpoint:
  `POST /api/servers/{id}/restart`
- Endpoint uses the configured systemd service for the selected BeamMP server.
- Deployed and verified BeamAdmin API health after restart.

## Vanilla+ Mods Work

- Researched official BeamNG resource mods and saved the list to:
  `/opt/beamadmin/mod_list.txt`
- Added the list to the GitHub repo as `mod_list.txt`.
- Downloaded official BeamNG resource zips locally because the VPS could not directly reach BeamNG/R2 download hosts.
- SCP uploaded downloaded zip files to the VPS.
- Initially moved the batch into Vanilla+ live client resources, then moved it to `/opt/beamadmin/mods`, then moved it back into Vanilla+ when requested.
- Final requested state:
  `/opt/beammp-vanilla-plus/Resources/Client`
- Final Vanilla+ client folder state at the time:
  `50` zip files, about `5.1G`
- `/opt/beamadmin/mods` was left empty after moving the batch back live.
- Corrected a bad CK Tire Pack resource URL and replaced the wrong downloaded zip:
  - Removed accidental `legran_hd_towing.zip`
  - Added correct `ck_tires_repo.zip`

## VPS / Services

- Restarted and verified:
  - `beamadmin`
  - `beammp-vanilla-plus`
- Reloaded Nginx after panel/API changes.
- Confirmed BeamAdmin API health with:
  `curl http://127.0.0.1:8091/api/health`

## GitHub Releases Created

- `v0.2.0` - Live players/admin reasons
- `v0.3.0` - Six management pages
- `v0.3.1` - Map restart fix
- `v0.3.2` - Vanilla+ mods page
- `v0.3.3` - Vanilla+ mod research list
- `v0.3.4` - Vanilla+ mod download report
- `v0.3.5` - Mod staging correction to `/opt/beamadmin/mods`
- `v0.3.6` - Mod batch moved back live into Vanilla+
- `v0.3.7` - Per-server restart controls

## Local PC Cleanup

- Closed unused apps/processes while keeping BeamNG, OBS, Discord, BeamMP, Steam, audio/camera utilities, and Windows services running.
- Closed large non-game apps including Chrome, Minecraft, Spotify, Epic Games, EA desktop app, Modrinth, WinRAR, WheelCheck, Remote Desktop, and extra dialogs.
- Left unsaved Notepad windows alone to avoid losing text.

## Notes

- Live IPs and admin passwords are intentionally not recorded in this repo note.
- Mod zips are installed on the VPS but are not committed to GitHub because they are large binary files.
